build/
dist/

# ReShade (user-installed, not shipped)
opengl32.dll
ReShade.ini
ReShade.log
ReShadePreset.ini
reshade-shaders/

# Stray runtime copies in repo root (belong in build/Release only)
/OpenCarnivores.exe
/OpenCarnivores.map
/SDL2.dll
/OpenAL32.dll
/display.cfg

# dgVoodoo2 wrapper (downloaded separately)
dgvoodoo2/
DDraw.dll
D3DImm.dll
dgVoodoo.conf
dgVoodooCpl.exe

# Runtime logs and saves
*.log
*.sav

# Screenshots (dev artifacts)
screenshot.png
test_screenshot.png

# SDL2 binary distribution — headers/libs committed, zip not needed
deps/SDL2-devel-*.zip

# SDL2 compiled binaries — libs committed for convenience but pdbs not needed
deps/SDL2-*/lib/**/*.pdb

# Kiro IDE spec/steering files
.kiro/

# VS/build artifacts
*.user
*.suo
*.ncb
*.sdf
.vs/
out/
Debug/

# Player-local mod install: user's installed mod packs and their enabled list.
# The mods.cfg is per-install and shouldn't be shared across repo clones.
mods/
mods.cfg
OpenCarnivores/
